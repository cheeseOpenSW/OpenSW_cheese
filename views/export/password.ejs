<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Protection</title>
    <link rel="stylesheet" href="/css/security.css">
    <!-- <link rel="stylesheet" href="/css/calendar/calendar_memory.css"> -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        function validatePassword(form) {
            var password = form.pass.value.trim();

            if (password === "1234") {
                location = "/diagnosis/export";
            } else {
                alert("패스워드가 틀립니다.");
            }
        }
    </script>
</head>
<body>
    <div class="security_wrapper">
        <div class="security_container">
            <div class="security_title">
                <p>해당 페이지는 비밀번호 입력으로 이용가능합니다.</p>
            </div>  
            <div class="security_form">
                <form name="loginForm">
                    <p style="margin-bottom:20px;"> 비밀번호 입력
                        <input name="pass" type="password" size="12">
                        <input type="button" class="security_Btn" value="확인" onClick="validatePassword(this.form)">
                    </p>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
